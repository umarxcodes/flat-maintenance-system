// =====================  CONSTANTS  =========================
/**
 * Blocks module configuration constants and validation boundaries.
 */
export const BLOCKS_CONSTANTS = Object.freeze({
  MIN_FLOORS: 0,
  MAX_FLOORS: 300,
  NAME_MIN_LENGTH: 1,
  NAME_MAX_LENGTH: 100,
  CODE_MAX_LENGTH: 50,
});
